import Permission, {IPermission} from './Permission';

// This file was autogenerated. Please do not change.
// All changes will be overwrited on commit.
export interface ICreateAPermissionRequest {
    permission: IPermission;
}

export default class CreateAPermissionRequest {
    readonly _permission: Permission;

    get permission(): Permission {
        return this._permission;
    }

    constructor(props: ICreateAPermissionRequest) {
        this._permission = new Permission(props.permission);
    }

    serialize(): ICreateAPermissionRequest {
        const data: ICreateAPermissionRequest = {
            permission: this._permission.serialize(),
        };
        return data;
    }

    validate(): string[] {
        const validate = {
            permission: this._permission.validate().length === 0,
        };
        const isError: string[] = [];
        Object.keys(validate).forEach((key) => {
            if (!(validate as any)[key]) {
                isError.push(key);
            }
        });
        return isError;
    }

    update(
        props: Partial<ICreateAPermissionRequest>,
    ): CreateAPermissionRequest {
        return new CreateAPermissionRequest({...this.serialize(), ...props});
    }
}
