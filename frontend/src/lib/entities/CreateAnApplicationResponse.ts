import Application, {IApplication} from './Application';

// This file was autogenerated. Please do not change.
// All changes will be overwrited on commit.
export interface ICreateAnApplicationResponse {
    application: IApplication;
}

export default class CreateAnApplicationResponse {
    readonly _application: Application;

    get application(): Application {
        return this._application;
    }

    constructor(props: ICreateAnApplicationResponse) {
        this._application = new Application(props.application);
    }

    serialize(): ICreateAnApplicationResponse {
        const data: ICreateAnApplicationResponse = {
            application: this._application.serialize(),
        };
        return data;
    }

    validate(): string[] {
        const validate = {
            application: this._application.validate().length === 0,
        };
        const isError: string[] = [];
        Object.keys(validate).forEach((key) => {
            if (!(validate as any)[key]) {
                isError.push(key);
            }
        });
        return isError;
    }

    update(
        props: Partial<ICreateAnApplicationResponse>,
    ): CreateAnApplicationResponse {
        return new CreateAnApplicationResponse({...this.serialize(), ...props});
    }
}
